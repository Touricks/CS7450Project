# experiments/

Time-scoped experiment runs. Convention: YYYY-MM-DD-short-name/.
Do NOT read entire directory contents unless specifically asked — protect context window.

Each experiment subdirectory should contain:
- README.md (hypothesis, status, which assets/ versions were used)
- inputs/ or data/
- results/
